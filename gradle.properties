# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# We no longer need to auto-convert third-party libraries to use AndroidX, which slowed down the build
android.enableJetifier=false

# Disable non-transitive R classes to allow library modules to see resources from their dependencies
android.nonTransitiveRClass=false

# Allow Gradle to use up to 1 GB of RAM
org.gradle.jvmargs=-Xmx1024M

# Use the compatible JDK 21 bundled with Android Studio
org.gradle.java.home=C:\\Program Files\\Android\\Android Studio\\jbr
